import React from 'react';

import ResponsiveWrapper from './../../components/ResponsiveWrapper'
const charts = () => (
	<ResponsiveWrapper 
		className="lineBox"
		dimensions={{w: 450, h: 250}}
	>
		<p>demo text here</p>
	</ResponsiveWrapper>

);

export default charts;
